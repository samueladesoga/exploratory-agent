# Required: your Anthropic API key
ANTHROPIC_API_KEY=sk-ant-...

# Per-client login credentials. The client YAML names which env vars to read,
# so credentials never live in config files and are never shown to Claude.
SAUCE_USERNAME=problem_user
SAUCE_PASSWORD=secret_sauce
